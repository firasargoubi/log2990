export enum PageUrl {
    Home = '/home',
    Edit = '/edit',
    Join = '/join',
    Create = '/create',
    Admin = '/admin',
    Waiting = '/waiting',
    Play = '/play',
}

export enum ApiEndpoints {
    AllGames = '/all',
    VisibleGames = '/visible',
    GameCreate = '/create',
    ValidateGame = '/validate',
}
