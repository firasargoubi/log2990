export enum PageUrl {
    Home = '/home',
    Edit = '/edit',
    Join = '/join',
    Create = '/create',
    Admin = '/admin',
    Waiting = '/waiting',
    Play = '/play',
    Stats = '/statistics',
}

export enum ApiEndpoints {
    AllGames = '/all',
    VisibleGames = '/visible',
    GameCreate = '/create',
    ValidateGame = '/validate',
}
