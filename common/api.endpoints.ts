export enum ApiEndpoint {
    AllGames = '/all',
    VisibleGames = '/visible',
    Create = '/create',
    Validate = '/validate',
    Send = '/send',
}

export enum ApiRoutes {
    Game = '/api/game',
    Example = '/api/example',
}